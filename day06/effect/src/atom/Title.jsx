const Title = ({ title }) => {
    return (
        <span
            style={{ fontSize: "20px", fontWeight: "bold", color: "#36d7b7" }}
        >
            {title}
        </span>
    );
};

export default Title;
