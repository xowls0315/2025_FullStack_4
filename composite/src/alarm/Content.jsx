const Content = ({ name }) => {
    return (
        <span>
            <strong>{name} </strong>
            started
            <br /> following you.
        </span>
    );
};

export default Content;
