const Place = ({ place }) => {
    return (
        <div style={{ margin: "0px 100px" }}>
            <span style={{ fontWeight: "bold", color: "gray" }}>{place}</span>
        </div>
    );
};

export default Place;
